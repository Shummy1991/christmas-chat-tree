// import { SET_WEBP } from "../actions/webp";

export default (user = {}, action) => {
    // const { payload } = action;
    switch (action.type) {
        default:
            return user;
    }
};
